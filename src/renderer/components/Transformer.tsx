import { Select } from "@mui/material";
import React from "react";
import Action from "./Transformer/Action";

interface Props {}

const Transformer = (props: Props) => {
  return (
    <div>
      <Action />
    </div>
  );
};

export default Transformer;
